# 🗺️ QMS v2 5대 도메인 작성 스킬 매핑 설계 및 적용 기획안 (R1)

* **작성일자:** 2026년 06월 03일
* **보고대상:** 신우밸브주식회사 품질보증부 전민재 차장님
* **작성자:** QMS AI 전담 비서 안티그래비티

---

## 1. 아키텍처 로직 구성 변경안 (AS-IS ➔ TO-BE)

현재 QMS 규칙 시스템은 문서의 아카이빙(저장, 리비전, 동기화)만 스킬로 위임하고 있으며, 각 문서의 실질적인 작성(Authoring) 단계는 지정된 스킬이 없이 에이전트의 임의 포맷에 의존하고 있습니다. 본 기획안은 .agent/skills에 내장된 기존 범용 스킬들을 QMS 5대 핵심 도메인에 명시적으로 매핑하고, QMS 제약사항(한글 작성, 저장경로 강제 오버라이드 등)을 각 스킬 및 규칙 파일에 강력하게 바인딩하는 것을 목표로 합니다.

```
[AS-IS: 작성 스킬 미지정 및 임의 형식 작성]
- DNAS 결재 문서 작성 시, 어떤 스킬을 사용하여 어떤 포맷으로 작성해야 하는지 강제 규정이 없음.
- superpower 계열 범용 스킬들이 기본 저장경로(docs/superpowers/plans/ 등)와 영문 작성 전제를 가지고 있어 QMS에 그대로 사용 시 오작동 및 경로 혼선 발생.

[TO-BE: 도메인별 작성 스킬 매핑 및 스킬 본문 보정]
- 규칙(02_dnas_process.md)에 도메인별 작성 스킬, 아카이빙 스킬, 자가검토 게이트 간의 표준 연계 순서 명문화.
- 4대 범용 스킬 정의서(SKILL.md) 내부에 "QMS 프로젝트 오버라이드 제약 사항"을 명시적으로 수술식 이식하여 스킬 동작 시 QMS 전용 경로 및 한글 작성을 완벽히 강제함.

   DNAS 문서 작성 요구
          │
          ├──➔ Plan (기획안)      ➔ writing-plans 스킬      ➔ [오버라이드] 안티그래비티\plan\ (한글)
          ├──➔ Task (작업 명세)    ➔ planning-with-files 스킬➔ [오버라이드] 안티그래비티\task\ (한글)
          ├──➔ Walkthrough (완료) ➔ internal-comms 스킬     ➔ [오버라이드] 안티그래비티\walkthrough\ (한글)
          └──➔ Report (특별 보고)  ➔ doc-coauthoring 스킬    ➔ [오버라이드] 안티그래비티\report\ (한글)
```

---

## 📐 Proposed Changes (세부 수정 명세 및 비포-애프터)

### 1. 규칙 정비 부문 (Rules Refactoring)

#### [MODIFY] [02_dnas_process.md](file:///c:/Users/mjjeon/Desktop/QMS 프로젝트/shinwoo-valve-qms/.agent/rules/02_dnas_process.md) (로컬 결재 룰)

* **## 1. 3단계 결재 프로세스 개편 (완료보고 4대 서식 정의)**:
  * **AS-IS (기존)**:
    ```markdown
    ## 1. 3단계 결재 프로세스 (DNAS)
    어떠한 경우에도 기능 추가/수정 진행 시 아래 3단계 공식 보고(Artifact 문서 송출) 절차를 절대 생략할 수 없다.
    1. **`Plan` (기획안 송출)**: 변경할 파일, 충돌 위험성을 분석하여 `implementation_plan` 규격으로 UI에 띄워서 사용자 컨펌(승인 키워드)을 받아야 한다.
    2. **`Task` (작업 명세 송출)**: 기획이 컨펌되면 실제 코드 수정 전 `task` 규격 문서로 Step을 마킹한다.
    3. **`Walkthrough` (최종 보고서)**: 개발 및 시운전이 끝나면 `walkthrough` 문서로 최종 결과를 브리핑한다.
    ```
  * **TO-BE (변경)**:
    ```markdown
    ## 1. 3단계 결재 프로세스 (DNAS) 및 스킬 연동 강령
    어떠한 경우에도 기능 추가/수정 진행 시 아래 3단계 공식 보고 절차와 전용 스킬 연동을 절대 생략할 수 없다.

    1. **`Plan` (기획안 송출)**: 
       - 변경할 파일, 충돌 위험성을 분석하여 `implementation_plan` 규격으로 UI에 띄워 차장님의 승인 결재를 받아야 한다.
       - 송출 직전 반드시 `plan-self-review` 스킬(경로: `.agent/skills/plan-self-review/SKILL.md`)을 실행하여 결함 0건을 검증해야 하며, 확정된 Plan은 `revision-archiver` 스킬(경로: `.agent/skills/revision-archiver/SKILL.md`)을 호출하여 `안티그래비티\plan\` 폴더에 리비전 파일로 아카이빙한다.

    2. **`Task` (작업 명세 송출)**: 
       - 기획이 컨펌되면 실제 코드 수정 전 `task` 규격 문서로 Step을 마킹한다.
       - 확정된 Task는 `revision-archiver` 스킬을 사용하여 `안티그래비티\task\` 폴더에 리비전 파일로 아카이빙한다.

    3. **`Walkthrough` (최종 보고서)**: 
       - 개발 및 시운전이 끝나면 최종 결과를 브리핑한다.
       - 완료보고서는 필수적으로 4대 표준 서식(① 반영 내역 요약, ② 반영 결과 요약, ③ 검증 계획 요약, ④ 검증 결과 요약)을 준수하여 작성하며, `revision-archiver` 스킬을 사용하여 `안티그래비티\walkthrough\` 폴더에 리비전 파일로 아카이빙한다.
    ```

* **## 4. 스킬 실행 안내 및 자동 위임 개편 (작성 스킬 매핑 및 연계 순서 주입)**:
  * **AS-IS (기존)**:
    ```markdown
    ## 4. 스킬 실행 안내 및 자동 위임
    - 본 프로젝트의 5대 핵심 도메인(Plan, Task, Walkthrough, Report, images) 관리 및 동기화는 아래 전용 스킬 정의서에 의해 물리적 제어가 수행된다:
      - Plan, Task, Walkthrough, Report 통합 아카이빙 ➔ `revision-archiver` 스킬(경로: `.agent/skills/revision-archiver/SKILL.md`)
      - 이미지 자동화 보관 ➔ `image-archiver` 스킬(경로: `.agent/skills/image-archiver/SKILL.md`)
      - 릴리즈 인트라넷 동기화 ➔ `release-sync` 스킬(경로: `.agent/skills/release-sync/SKILL.md`)
      - **기획안 제출 전 자가검토 (필수)** ➔ `plan-self-review` 스킬(경로: `.agent/skills/plan-self-review/SKILL.md`) — Plan 작성 완료 후 차장님 송출 직전 반드시 실행, 높음 등급 이슈 0건 확인 후에만 제출 허용
    ```
  * **TO-BE (변경)**:
    ```markdown
    ## 4. 스킬 실행 안내 및 자동 위임

    ### 4-1. 도메인별 작성(Authoring) 스킬 — 작성 시 반드시 호출
    - 본 프로젝트의 문서 4종은 작성 시점에 아래 전용 스킬을 호출하여 작성한다.
      단, 각 스킬의 기본 저장경로·언어·전제는 무시하고, 아래 QMS 규격을 강제 우선한다.
      - **Plan (기획안)** ➔ `writing-plans` 스킬 / 저장 `안티그래비티\plan\` / 한글 implementation_plan 형식
      - **Task (작업 명세)** ➔ `planning-with-files` 스킬 / 저장 `안티그래비티\task\` / 한글 DNAS YYYY-MM-DD_[과업주제]_R[N].md 표준 및 task Step 형식
      - **Walkthrough (완료 보고)** ➔ `internal-comms` 스킬 / 저장 `안티그래비티\walkthrough\` / 한글 DNAS YYYY-MM-DD_[주제명]_R[N].md 표준 및 완료보고 4대 표준 서식
      - **Report (특별 분석 보고)** ➔ `doc-coauthoring` 스킬 / 저장 `안티그래비티\report\` / 한글 DNAS YYYY-MM-DD_[주제명]_R[N].md 표준 및 분석보고 형식

    ### 4-2. 작성 후 관리·동기화 위임 (기존 유지)
      - Plan/Task/Walkthrough/Report 리비전 관리 ➔ `revision-archiver` 스킬
      - 이미지 자동화 보관 ➔ `image-archiver` 스킬
      - 릴리즈 인트라넷 동기화 ➔ `release-sync` 스킬
      - 기획안 제출 전 자가검토(필수) ➔ `plan-self-review` 스킬

    ### 4-3. 표준 연계 순서
      - Plan: writing-plans 작성 ➔ plan-self-review(High 0건) ➔ revision-archiver(버전)
      - Task: planning-with-files 작성 ➔ revision-archiver
      - Walkthrough: internal-comms 작성 ➔ revision-archiver ➔ release-sync(동기화)
      - Report: doc-coauthoring 작성 ➔ revision-archiver
    ```

---

### 2. 스킬 정비 부문 (Skills Refactoring)

#### [MODIFY] [writing-plans/SKILL.md](file:///c:/Users/mjjeon/Desktop/QMS 프로젝트/shinwoo-valve-qms/.agent/skills/writing-plans/SKILL.md)
* **QMS 오버라이드 제약사항 주입**:
  * **저장경로**: `docs/superpowers/plans/` ➔ `안티그래비티\plan\` 폴더에 `YYYY-MM-DD_[주제명]_R[N].md` 리비전 파일로 아카이빙.
  * **양식 및 언어**: 반드시 한글(한국어)로 작성하며, `implementation_plan` 서식을 준수. TDD, Git Commit/PR 강제 조항 대신 DNAS 절차 최우선.

#### [MODIFY] [planning-with-files/SKILL.md](file:///c:/Users/mjjeon/Desktop/QMS 프로젝트/shinwoo-valve-qms/.agent/skills/planning-with-files/SKILL.md) (A항 해결 - hooks 제거 명문화)
* **전면적인 hooks 제거/비활성화**:
  * 스킬 frontmatter 내 정의된 `hooks:` 블록(`UserPromptSubmit / PreToolUse / PostToolUse / Stop / PreCompact` 하위의 모든 자동 실행 셸 스크립트 블록)을 **통째로 제거 또는 비활성화**하여 QMS 결재 흐름에 간섭하는 터널 오작동을 원천 차단합니다.
* **QMS 오버라이드 제약사항 주입**:
  * **저장경로**: `.planning/` 및 root ➔ `안티그래비티\task\` 폴더에 `YYYY-MM-DD_[과업주제]_R[N].md` 표준 명명법으로 리비전 파일 아카이빙.
  * **양식 및 언어**: 한글로 작성하고 QMS DNAS 표준 task Step 형식으로 가공 출력.

#### [MODIFY] [internal-comms/SKILL.md](file:///c:/Users/mjjeon/Desktop/QMS 프로젝트/shinwoo-valve-qms/.agent/skills/internal-comms/SKILL.md)
* **QMS 오버라이드 제약사항 주입**:
  * **저장경로**: default ➔ `안티그래비티\walkthrough\` 폴더에 `YYYY-MM-DD_[주제명]_R[N].md` 리비전 파일로 아카이빙.
  * **양식 및 언어**: 한글 완료보고 4대 표준 서식(반영 내역, 반영 결과, 검증 계획, 검증 결과 요약)을 강력히 준수하여 작성.

#### [MODIFY] [doc-coauthoring/SKILL.md](file:///c:/Users/mjjeon/Desktop/QMS 프로젝트/shinwoo-valve-qms/.agent/skills/doc-coauthoring/SKILL.md)
* **QMS 오버라이드 제약사항 주입**:
  * **저장경로**: default ➔ `안티그래비티\report\` 폴더에 `YYYY-MM-DD_[주제명]_R[N].md` 리비전 파일로 아카이빙.
  * **양식 및 언어**: 한글 격식 작성 및 한글 DNAS 분석 보고(Report) 포맷을 우선 적용.

---

## 🧪 Verification Plan (검증 계획 - D항 연계 시나리오 및 C항 파일명 정합성 추가)

### 1. 도메인별 작성 스킬 시뮬레이션 및 교차 검수
* **Plan**: `writing-plans` 스킬 구동 시 `안티그래비티\plan\` 폴더에 한글 `implementation_plan` 규격으로 문서가 정확히 생성되는지 확인하고, 송출 전 `plan-self-review`를 실행하여 결함이 0건임을 통과하는 연계 동작 검증.
* **Task (C항 반영)**: `planning-with-files` 스킬 구동 시 `안티그래비티\task\` 폴더에 명명 표준인 `YYYY-MM-DD_[과업주제]_R[N].md` 형식으로 파일이 정확하게 생성되며, `revision-archiver`에 의해 버전이 정상 관리되는지 체크.
* **Walkthrough**: `internal-comms` 스킬 구동 시 `안티그래비티\walkthrough\` 폴더에 4대 표준 서식을 완벽히 적용하여 완료보고서가 생성되는지 확인하고, `release-sync` 스킬이 실행되어 동기화가 연계되는지 체크.
* **Report**: `doc-coauthoring` 스킬 구동 시 `안티그래비티\report\` 폴더에 한글 분석보고서 규격 및 명명 표준으로 아카이빙되는지 체크.
