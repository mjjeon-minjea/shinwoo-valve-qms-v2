# 저장소 전수점검 후속조치 구현계획서 (R3 - 최종 승인)

본 계획서는 `클로드\Report\2026-06-06_저장소_전수점검_문제점_종합보고서_R0.md` 보고서의 점검 결과 및 차장님의 최종 피드백과 실무 권고사항을 완벽히 반영하여 최종 확정된 구현계획서입니다.
전민재 차장님의 최종 승인을 획득하였으며, **시크릿 키 회전 조치(Supabase keys, DB Password, Gemini API Key, Vercel OIDC Token 등의 재발급)는 제외**하고 진행합니다.

---

## User Review Required (최종 승인 완료)

> [!CAUTION]
> **[A] 시크릿 키 미회전 및 히스토리 유지에 따른 잔존 위험성 명시**
> * 차장님 지침에 따라 키 회전 및 `git filter-repo`(히스토리 영구 정화)는 수행하지 않습니다.
> * 이로 인해 과거 커밋 히스토리에 노출된 `SUPABASE_SERVICE_ROLE_KEY`(RLS 우회 전체권한), `PROD_DB_URL`, `STAGING_DB_URL`, `GEMINI_API_KEY` 등은 git history(`git show [commit_hash]`)를 통해 언제든지 조회가 가능한 상태로 남게 됩니다.
> * **차장님께서 이 잔존 위험성을 인지하셨으며, 로컬 및 배포 환경의 보안 통제 하에 "미회전 및 과거 히스토리 유지" 상태로 진행하는 것을 최종 승인(Accept)하셨습니다.**

> [!IMPORTANT]
> **[C] `.obsidian/` 내 위키 콘텐츠 및 `GEMINI.md` 영구 보존**
> * `.obsidian/` 하위의 개인 설정 파일들과 캐시 폴더는 확실히 무시하되, 실제 위키 콘텐츠 파일(`.obsidian/wiki/`)과 `.obsidian/GEMINI.md` 파일은 절대 추적 해제되거나 삭제되지 않도록 무시 목록에서 제외하고 안전하게 보호합니다.
> * 단, 파이프라인 생성물인 `.obsidian/raw/GEMINI.md` 및 `.obsidian/output/GEMINI.md`는 계획대로 정상 추적 해제 처리합니다.

---

## 실무 조치 지침 (Execution Details)

1. **실터미널 한글 파일명 인식 처리**
   * 윈도우 환경 한글 인코딩에 의한 git rm pathspec 에러 방지를 위해, 실터미널 실행 시 `git config core.quotepath false`를 적용한 후 와일드카드(`*`) 패턴을 사용해 안전하게 제거합니다.
2. **`vite.config.js`의 프록시 설정 중앙화 제외**
   * 로컬 개발 전용 하드코딩 매핑이므로 중앙화 범위에서 명시적으로 제외하고 유지합니다.
3. **`import.meta.env.DEV` 가드 조건 보존**
   * `DevNotes.jsx`에서 prod용 Supabase fetch 분기가 훼손되지 않도록 `if (import.meta.env.DEV)` 가드를 철저하게 유지한 상태에서 내부 로컬 주소 조회 부분만 새로 생성할 `config.js` 설정을 참조하도록 변경합니다.

---

## Proposed Changes

### 1. `.gitignore` 보강 및 무시-해제 일치성 정비 (Git Hygiene)

#### [MODIFY] [.gitignore](file:///c:/Users/mjjeon/Desktop/QMS%20프로젝트/shinwoo-valve-qms/.gitignore)
```gitignore
# Env (환경변수 변형 전체 차단)
.env*
!.env.example
*.bak

# 데이터 덤프 / 백업 (실제 데이터 파일만 무시)
db.json
dev_notes_rows.json
*_backup.json
src/data/mes_process_inspections.json

# 빌드 로그
build*.log
build*.txt

# 생성 캐시/그래프 산출물
graphify-out/

# 스크래치 폴더 전체 무시
scratch/

# 옵시디언 개인 에디터 설정 및 캐시 선별 무시 (wiki 및 GEMINI.md 보존)
.obsidian/*.json
.obsidian/*.MD
.obsidian/*.md
.obsidian/.obsidian/
.obsidian/plugins/
.obsidian/raw/
.obsidian/output/
.obsidian/graphify-out/
!.obsidian/wiki/
!.obsidian/GEMINI.md
```

#### [NEW] [.env.example](file:///c:/Users/mjjeon/Desktop/QMS%20프로젝트/shinwoo-valve-qms/.env.example)
향후 개발 환경 설정을 위한 실제 값이 비워진 환경변수 명세서 템플릿입니다.

#### [DELETE] git 추적 해제 대상 (Working Tree 파일은 보존, git index에서만 제거)
* **환경변수 변형**: `.env.local.bak`, `.env.preview`, `.env.vercel`, `.env.production.vercel`
* **데이터 덤프**: `db.json`, `dev_notes_rows.json`, `src/data/mes_process_inspections.json`, `scripts/staging_users_backup.json`, `scripts/staging_weekly_reports_backup.json`
* **임시 생성물 및 캐시**: `graphify-out/` 하위 전체 파일
* **스크래치**: `scratch/` 디렉토리 하위의 파일 전체
* **옵시디언 설정 및 캐시 (일치화 완료)**:
  * `.obsidian/*.json` (최상위 json 15개 전체 및 중첩 `.obsidian/` 폴더 내 5개 json 포함)
    * `app.json`, `appearance.json`, `core-plugins.json`, `workspace.json`, `clipper-01-article.json` ~ `clipper-10-course.json` 및 `기본-템플릿-clipper.json` (와일드카드 활용)
  * `.obsidian/나의 핵심 맥락.MD` (와일드카드 활용)
  * `.obsidian/raw/`, `.obsidian/output/`, `.obsidian/graphify-out/` 폴더 내 파일 전체

---

### 2. 소스코드 내 `localhost:3001` 하드코딩 설정 중앙화

#### [NEW] [config.js](file:///c:/Users/mjjeon/Desktop/QMS%20프로젝트/shinwoo-valve-qms/src/config/config.js)
```javascript
// Local Development API URL 중앙화 설정
export const LOCAL_API_URL = import.meta.env.VITE_LOCAL_API_URL || 'http://localhost:3001';
```

#### [MODIFY] [PostApproval.jsx](file:///c:/Users/mjjeon/Desktop/QMS%20프로젝트/shinwoo-valve-qms/src/components/PostApproval.jsx)
`http://localhost:3001` 대신 `LOCAL_API_URL` 상수를 로드하여 사용합니다.

#### [MODIFY] [DevNotes.jsx](file:///c:/Users/mjjeon/Desktop/QMS%20프로젝트/shinwoo-valve-qms/src/components/DevNotes.jsx)
`import.meta.env.DEV` 가드 내부의 호출부를 `LOCAL_API_URL` 상수를 로드하여 교체하며, DEV 가드 조건식 자체는 원본대로 보존합니다.

---

### 3. 중복 및 정리 대상 파일 정화

#### [DELETE] [HANDOFF.md](file:///c:/Users/mjjeon/Desktop/QMS%20프로젝트/shinwoo-valve-qms/HANDOFF.md)
루트의 중복 문서를 지우고 `shinwoo-system/HANDOFF.md`를 단일 진실 원천(SSOT)으로 사용합니다.

#### [DELETE] [clean_obsidian_wiki.js](file:///c:/Users/mjjeon/Desktop/QMS%20프로젝트/shinwoo-valve-qms/scratch/clean_obsidian_wiki.js)
중복되는 JS 파일을 정리합니다. (단, `scratch/` 디렉토리가 추적 해제되므로 git 상에서만 정리됩니다.)

---

## Verification Plan

### Automated Tests
* `npm run build`를 구동하여 코드 변경 및 설정 이관 후 정상 빌드되는지 확인합니다.
* `git ls-files`를 돌려 무시한 설정 및 캐시 파일들이 git 추적 목록에서 완벽히 배제되었는지, 그리고 중요 위키 파일(`.obsidian/wiki/`) 및 `.obsidian/GEMINI.md`는 추적이 여전히 활성화되어 있는지 확인합니다.

### Manual Verification
* `git rm --cached` 실행 후, 로컬의 중요 데이터 파일들이 물리 디렉토리에서 보존되었음을 직접 확인합니다.
* 로컬 개발 서버를 실행하여 프론트엔드에서 API 연동 기능이 정상 작동하는지 확인합니다.
