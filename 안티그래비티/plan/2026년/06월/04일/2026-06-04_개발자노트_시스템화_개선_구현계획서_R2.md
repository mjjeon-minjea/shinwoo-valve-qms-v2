# 📋 QMS v2 개발자 노트 시스템화 개선 구현계획서 (R2)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 로컬 DNAS 통제 무결성 강화 및 작성일 백데이트(수동 지정), 반려 검증 우회 편법을 제거하는 개발자 노트 시스템 개선

**Architecture:** 
1. `server.js` 기존 미들웨어 검증기 블록 내부를 직접 수정하여, `status === 'rejected'`인 경우 DNAS 키워드 검증을 Pass(next)하도록 구성합니다.
2. `PostApproval.jsx` 반려 시 우회 텍스트 강제 주입을 완전 제거하고, 승인 버튼 클릭 시 날짜 지정(`type="date"`) 및 확인을 처리하는 **승인 확인 미니 모달**을 띄워 날짜 깨짐 및 정렬 오류를 예방하는 동시에 깔끔한 백데이트 처리를 가능하게 합니다.
3. 빌드 크래시 실험 오염 방지를 위해 `node_modules`를 배제하는 PowerShell `robocopy` 명령어 기반의 깨끗한 대조군 빌드 검증 단계를 선행합니다.

**Tech Stack:** Node.js, Express, React (Vite), Supabase Client

---

## 🔍 User Review Required

> [!IMPORTANT]
> **로컬 미들웨어(`server.js`) 직접 수정 수반**:
> 로컬 DNAS Validator의 예외 처리를 위해 JSON Server 구동 파일의 수정이 이루어집니다. 이는 로컬 API 서버 개발 환경에만 직접 반영됩니다.

> [!NOTE]
> **P4 (`is_synced` 클라우드 미기록) 과제 이월**:
> 본 R2 개선 범위에서는 2순위(P1, P2, P5)의 치명적 결함 해결에 집중하며, Supabase 클라우드 상태 컬럼 추가 및 풀싱크 복구 스크립트 작성(P4)은 안정화 단계 이후 차기 이력 관리 이정표로 명시적으로 이월 처리합니다.

---

## 🛠️ Proposed Changes

### Task 1: 🛡️ 로컬 미들웨어 `server.js` 반려 상태 DNAS 검증 예외 처리

**Files:**
- Modify: [server.js](file:///c:/Users/mjjeon/Desktop/QMS 프로젝트/shinwoo-valve-qms/server.js:48-67)

- [ ] **Step 1: `server.js` 기존 DNAS Validator 미들웨어 내부에 예외 분기 추가**

수정 대상 코드 (`server.js` 48~67행):
```javascript
// AS-IS:
server.use((req, res, next) => {
    if ((req.method === 'POST' || req.method === 'PATCH' || req.method === 'PUT') && 
        (req.path === '/dev_notes' || req.path.startsWith('/dev_notes/'))) {
        
        // 데이터 본문(content) 수정이 포함된 요청에만 DNAS 포맷 검증 수행
        if (req.body.content !== undefined) {
            const content = req.body.content || '';
            // ...

// TO-BE: (server 인스턴스 사용 및 content 판단 전 status === 'rejected' 예외 삽입)
server.use((req, res, next) => {
    if ((req.method === 'POST' || req.method === 'PATCH' || req.method === 'PUT') && 
        (req.path === '/dev_notes' || req.path.startsWith('/dev_notes/'))) {
        
        // 💡 반려(rejected) 상태의 패치노트는 필수 품질 검증 키워드 검사 제외
        if (req.body && req.body.status === 'rejected') {
            return next();
        }

        // 데이터 본문(content) 수정이 포함된 요청에만 DNAS 포맷 검증 수행
        if (req.body.content !== undefined) {
            const content = req.body.content || '';
            // ...
```

- [ ] **Step 2: 로컬 서버 정상 가동 및 API 응답 확인**

JSON Server를 구동한 뒤 4대 키워드가 누락된 반려 데이터 PATCH 요청이 400 Bad Request를 뿜지 않고 통과하는지 백그라운드 테스트로 검증합니다.

---

### Task 2: 🚫 `PostApproval.jsx` 반려 처리 내 편법 우회 코드 제거

**Files:**
- Modify: [PostApproval.jsx](file:///c:/Users/mjjeon/Desktop/QMS 프로젝트/shinwoo-valve-qms/src/components/PostApproval.jsx:182-207)

- [ ] **Step 1: `handleReject` 내 content 꼼수 텍스트 주입 제거**

수정 대상 코드 (`PostApproval.jsx` 191~195행):
```javascript
// AS-IS:
content: `### 🚫 반려됨 - 재작업 필요\n**사유: ${reason}**\n*(원인: 반려 재작성 / 대책: 본문 보완 / 결과: rejected / 물리적 증빙: 반려 통보)*\n\n---\n\n${target?.content || ''}`

// TO-BE: (순수 반려 사유 안내만 텍스트로 보존)
content: `### 🚫 반려됨 - 재작업 필요\n**사유: ${reason}**\n\n---\n\n${target?.content || ''}`
```

- [ ] **Step 2: 로컬 UI 반려 기능 작동 테스트**

대기열의 패치노트를 반려하고, 로컬 DB에 꼼수 키워드 텍스트가 강제 삽입되지 않고 깨끗하게 반려 상태가 갱신되는지 확인합니다.

---

### Task 3: 📅 `PostApproval.jsx` 승인 확인 날짜 지정 미니 모달 개발 및 백데이트 반영 (구 handleApprove 삭제 포함)

**Files:**
- Modify: [PostApproval.jsx](file:///c:/Users/mjjeon/Desktop/QMS 프로젝트/shinwoo-valve-qms/src/components/PostApproval.jsx)

- [ ] **Step 1: 승인 모달 상태(State) 및 선택 날짜 State 추가**

`PostApproval` 컴포넌트 시작 부분에 state 선언:
```javascript
const [approvingNote, setApprovingNote] = useState(null); // 승인 중인 노트 객체
const [approvalDate, setApprovalDate] = useState(''); // 선택된 배포일 (YYYY-MM-DD)
```

- [ ] **Step 2: 승인 버튼 클릭 시 모달 오픈 및 날짜 선택 UI 구성**

기존 `handleApprove`를 바로 띄우던 버튼의 이벤트를 상태 변환으로 교체합니다:
```html
// 버튼 이벤트 변경
onClick={() => {
    setApprovingNote(item);
    setApprovalDate('');
}}
```

화면 하단에 **승인 확인 미니 모달**을 구현합니다:
```html
{approvingNote && (
    <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-900/50 backdrop-blur-sm">
        <div className="bg-white rounded-xl shadow-xl w-full max-w-md overflow-hidden flex flex-col">
            <div className="p-5 border-b bg-slate-50">
                <h3 className="text-lg font-bold text-slate-800">개발자 노트 승인 및 배포일 지정</h3>
                <p className="text-xs text-slate-500 mt-1">버전: {approvingNote.version}</p>
            </div>
            <div className="p-5 flex flex-col gap-4">
                <div className="flex flex-col gap-1.5">
                    <label className="text-xs font-semibold text-slate-600">배포일 수동 지정 (미선택 시 오늘 날짜)</label>
                    <input 
                        type="date"
                        value={approvalDate}
                        onChange={(e) => setApprovalDate(e.target.value)}
                        className="w-full px-3 py-2 border border-slate-200 rounded-lg text-sm bg-white text-slate-700 focus:outline-none focus:ring-2 focus:ring-primary-500"
                    />
                </div>
                <p className="text-xs text-amber-600 bg-amber-50 p-2.5 rounded border border-amber-200">
                    ⚠️ 날짜 포맷은 시맨틱 버전 정렬을 보장하기 위해 'YYYY-MM-DD 12:00' 형식으로 기존 날짜 데이터 정합성과 완전히 일치하도록 저장 처리됩니다.
                </p>
            </div>
            <div className="p-4 border-t bg-slate-50 flex justify-end gap-2">
                <button 
                    onClick={() => setApprovingNote(null)} 
                    className="px-4 py-2 bg-slate-100 rounded text-sm text-slate-600 hover:bg-slate-200"
                >
                    취소
                </button>
                <button 
                    onClick={() => handleExecuteApprove(approvingNote, approvalDate)} 
                    className="px-4 py-2 bg-emerald-500 text-white rounded text-sm hover:bg-emerald-600 font-medium"
                >
                    최종 승인
                </button>
            </div>
        </div>
    </div>
)}
```

- [ ] **Step 3: 최종 승인 실행 함수 (`handleExecuteApprove`) 개발 및 DEV 가드 복원**

```javascript
// 기존 구 handleApprove (50~89행)를 완전히 삭제하고, 해당 위치에 handleExecuteApprove를 작성하여 데드코드를 정리합니다.
const handleExecuteApprove = async (note, customDate) => {
    // 💡 [DEV 환경 전용 가드] 실서버 배포 에러 방지
    if (!import.meta.env.DEV) {
        alert('승인 및 시스템 관리 권한은 로컬 인트라넷(localhost) 환경에서만 수행 가능합니다.');
        return;
    }

    // 🚨 DNAS 포맷 사전 검증
    const content = note.content || '';
    const requiredKeywords = ['원인', '대책', '결과', '물리적 증빙'];
    const missing = requiredKeywords.filter(kw => !content.includes(kw));
    if (missing.length > 0) {
        alert(`[DNAS 통제 발동] 본문 내 필수 품질 검증 항목이 누락되어 승인할 수 없습니다.\n누락된 키워드: ${missing.join(', ')}`);
        return;
    }

    // YYYY-MM-DD 12:00 또는 오늘 날짜 포맷 변환
    const finalDate = customDate 
        ? `${customDate} 12:00` 
        : new Date().toLocaleString('ko-KR', { 
            year: 'numeric', month: '2-digit', day: '2-digit', 
            hour: '2-digit', minute: '2-digit', hour12: false 
        }).replace(/\. /g, '-').replace(/\./g, '');

    setLoading(true);
    try {
        const res = await fetch(`http://localhost:3001/dev_notes/${note.id}`, {
            method: 'PATCH',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
                status: 'published',
                date: finalDate
            })
        });

        if (!res.ok) throw new Error('로컬 DB 반영 실패');
        alert('✅ 로컬 승인 완료! 로컬 개발자 노트 게시판에서 확인 가능합니다.');
        setApprovingNote(null);
        setSelectedNote(null);
        await loadData();
    } catch (err) {
        alert('승인 실패: ' + err.message);
    } finally {
        setLoading(false);
    }
};
```

---

### Task 4: 👤 담당자 필드 단일화 (`author`) 및 manager 데드코드 정리

**Files:**
- Modify: [DevNotes.jsx](file:///c:/Users/mjjeon/Desktop/QMS 프로젝트/shinwoo-valve-qms/src/components/DevNotes.jsx)
- Modify: [PostApproval.jsx](file:///c:/Users/mjjeon/Desktop/QMS 프로젝트/shinwoo-valve-qms/src/components/PostApproval.jsx)

- [ ] **Step 1: `DevNotes.jsx`에서 `item.manager` 관련 폴백 삭제**

```javascript
// AS-IS:
<td className="px-6 py-4 text-center text-sm text-slate-600">{item.author || item.manager || '시스템'}</td>

// TO-BE:
<td className="px-6 py-4 text-center text-sm text-slate-600">{item.author || 'Antigravity'}</td>
```

- [ ] **Step 2: `PostApproval.jsx` 내 `item.author || 'AI'`를 `item.author || 'Antigravity'`로 보정**

---

### Task 5: 🧪 빌드 오염 없는 영문 임시 경로 빌드 검증 선행 (`robocopy` 교체)

- [ ] **Step 1: `robocopy` 디렉터리 제외 옵션으로 순수 소스 복사**

파워쉘 명령어를 통해 임시 디렉토리를 정리한 후 `node_modules`를 확실하게 격리하여 복사합니다.

```powershell
# 기존 임시 디렉터리가 있을 경우 완전 정리
Remove-Item -Path "C:\Users\mjjeon\Desktop\qms_build_temp" -Recurse -Force -ErrorAction SilentlyContinue

# robocopy를 사용하여 node_modules, dist, .git을 완전히 제외하고 복사 실행
robocopy "C:\Users\mjjeon\Desktop\QMS 프로젝트\shinwoo-valve-qms" "C:\Users\mjjeon\Desktop\qms_build_temp" /E /XD node_modules dist .git

# .env.local 설정 보증
Copy-Item -Path "C:\Users\mjjeon\Desktop\QMS 프로젝트\shinwoo-valve-qms\.env.local" -Destination "C:\Users\mjjeon\Desktop\qms_build_temp\" -Force
```

- [ ] **Step 2: 임시 경로에서 깨끗한 환경의 `npm install` 실행**

```powershell
# 임시 폴더에서 패키지 설치
npm install
```

- [ ] **Step 3: 빌드 구동 및 원인 진단**

```powershell
npm run build
```
성공 및 실패 로그를 분석하여 한글 절대경로 인코딩 충돌 원인을 최종 확정합니다.

---

## 🧪 Verification Plan

### 수동 검증 프로토콜
1. **반려 DNAS 예외 테스트**: 로컬 대기열 글 하나를 임의로 반려하여 400 에러 없이 반려 처리와 데이터 갱신 완료 여부 검증.
2. **백데이트 승인 테스트**: 승인 단추 클릭 시 열리는 미니 모달에서 수동 날짜(`type="date"`)를 지정한 후 완료했을 때, 포맷이 `YYYY-MM-DD 12:00` 으로 안전하게 저장 및 목록 정렬 정상 작동 검증.
3. **빌드 원인 확정**: 영문 복사 빌드의 성공 로그를 수집하여 빌드 제한 요인 분석 완료.
