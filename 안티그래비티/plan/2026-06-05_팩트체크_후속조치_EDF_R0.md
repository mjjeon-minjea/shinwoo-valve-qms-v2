# 팩트체크 후속 조치 — [E] 테이블 재작업 + [D][F] 절차 보완

> **For agentic workers:** GEMINI.md §2 DNAS 결재 프로세스를 준수하여 차장님 승인 후 실행한다.

**Goal:** 팩트체크 보고서에서 적발된 [E] clone-db 테이블 불일치를 실측 스키마 기준으로 재작업하고, [D] DDL 미승인 및 [F] 비번 로테이션 미이행을 절차적으로 보완한다.

**Architecture:** clone-db.js의 `TARGET_TABLES` 배열을 메인·테스트 DB 양쪽에 실존하는 10종으로 교정 후 재실행. DDL 승인 게이트와 비번 로테이션은 코드 변경 없이 운영 절차만 명문화.

**Tech Stack:** Node.js, Supabase REST API, dotenv

**근거:** [2026-06-05_검증보고서_팩트체크_R0.md](file:///c:/Users/mjjeon/Desktop/QMS%20프로젝트/shinwoo-valve-qms/안티그래비티/report/2026-06-05_검증보고서_팩트체크_R0.md)

---

## 실측 스키마 대조 (2026-06-05 15:19 실측)

| 테이블 | 메인 DB | 테스트 DB | 현행 코드 | 확정안 |
|--------|:-------:|:---------:|:---------:|:------:|
| `users` | ✅ | ✅ | ✅ | ✅ |
| `dev_notes` | ✅ | ✅ | ❌ | ✅ |
| `notices` | ✅ | ✅ | ❌ | ✅ |
| `weekly_reports` | ✅ | ✅ | ✅ | ✅ |
| `process_inspections` | ✅ | ✅ | ✅ | ✅ |
| `inspections` | ✅ | ✅ | ✅ | ✅ |
| `item_master` | ✅ | ✅ | ✅ | ✅ |
| `inquiries` | ✅ | ✅ | ✅ | ✅ |
| `suggestions` | ✅ | ✅ | ✅ | ✅ |
| `settings` | ✅ | ✅ | ❌ | ✅ |
| `calendar_events` | ✅ | ❌ | ✅ | ⬜ 제외 |
| `resources` | ✅ | ❌ | ✅ | ⬜ 제외 |

> **확정안**: 메인·테스트 양쪽에 모두 존재하는 **10개 테이블** 전부 복제. `calendar_events`·`resources`는 테스트 DB에 미존재이므로 제외.

---

## 잠긴 표면 (GEMINI.md §5-1)

본 플랜은 아래 잠긴 표면을 **수정하지 않음**:
- `.eslintrc.cjs`
- `.agent/rules/GEMINI.md` §5 하네스 제약 섹션
- `.agent/skills/qms-orchestrator/scripts/verify-integration.js`
- `.agent/skills/qms-orchestrator/scripts/check-structure.js`

---

### Task 1: [E] clone-db.js TARGET_TABLES 교정

**Files:**
- Modify: `scripts/clone-db.js:59-69`

- [ ] **Step 1: 현행 TARGET_TABLES 배열 확인**

```powershell
Get-Content scripts\clone-db.js | Select-Object -Skip 58 -First 11
```

- [ ] **Step 2: TARGET_TABLES를 확정안 10종으로 교체**

```javascript
// scripts/clone-db.js (59행~)
const TARGET_TABLES = [
  'users',
  'dev_notes',
  'notices',
  'weekly_reports',
  'process_inspections',
  'inspections',
  'item_master',
  'inquiries',
  'suggestions',
  'settings',
];
```

- [ ] **Step 3: clone-db 재실행**

```powershell
node scripts/clone-db.js
```

Expected: 10종 테이블 복제 로그 출력, 메인 DB 쓰기 0건.

- [ ] **Step 4: 복제 결과 대조 검증**

```powershell
node -e "const d=require('dotenv');d.config({path:'.env.local'});const url=process.env.VITE_SUPABASE_URL+'/rest/v1/';const key=process.env.SUPABASE_SERVICE_ROLE_KEY;['users','dev_notes','notices','weekly_reports','process_inspections','inspections','item_master','inquiries','suggestions','settings'].forEach(t=>fetch(url+t+'?select=count',{headers:{apikey:key,Authorization:'Bearer '+key,Prefer:'count=exact'}}).then(r=>{const c=r.headers.get('content-range');console.log(t+': '+c)}))"
```

Expected: 각 테이블 건수 출력.

---

### Task 2: [D] DDL 승인 게이트 주석 명기

**Files:**
- Modify: `scripts/clone-db.js:1-5` (상단 주석)

- [ ] **Step 1: 파일 상단에 DDL 승인 규칙 주석 추가**

```javascript
// scripts/clone-db.js
// ⚠️ 운영 규칙: 테스트 DB 포함 모든 스키마 변경(ALTER/CREATE/DROP)은
//    대화창에서 차장님 사전 승인 후에만 실행한다. (GEMINI.md §5-2)
```

---

### Task 3: [F] 메인 비번 로테이션 리마인드

코드 변경 없음. 차장님 수동 영역.

- [ ] **Step 1: 리마인드 전달**

> Supabase 대시보드 → Settings → Database → Database password → Reset
> 메인 service-role 키가 clone-db 실행에 사용되었으므로 로테이션 권고.

---

## 검증 계획

| # | 검증 항목 | 방법 |
|---|----------|------|
| 1 | TARGET_TABLES 일치 | 코드 내 배열이 확정안 10종과 정확히 일치하는지 diff 확인 |
| 2 | clone-db 재실행 성공 | 10종 테이블 복제 로그 + 메인 DB 쓰기 0건 |
| 3 | 빌드 무결성 유지 | `npx vite build` 성공 |

## 롤백 방법

| Task | 롤백 |
|------|------|
| Task 1 | `git checkout scripts/clone-db.js` → 원본 복원 |
| Task 2 | 주석 삭제 (비파괴적) |
| Task 3 | 해당 없음 (수동 절차) |
