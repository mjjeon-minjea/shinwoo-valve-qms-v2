# 저장소 전수점검 후속조치 작업 명세서 (R0 - 완료)

본 작업 명세서는 최종 승인된 `2026-06-06_저장소_전수점검_후속조치_R3.md` 구현계획서에 따라 단계를 나누어 실행 상태를 추적합니다.

---

## 📋 작업 체크리스트

### Phase 1. Git Hygiene (환경 정비 및 .gitignore 보강)
- [x] `.gitignore` 파일 보강 수정 (시크릿 변형, 덤프, 캐시, scratch/, .obsidian/ 설정 무시)
- [x] `.env.example` 명세 템플릿 신규 생성 및 git 추적 추가
- [x] 실터미널에서 `git config core.quotepath false` 실행
- [x] 환경변수 변형 파일 4종 git 추적 해제
  - `git rm --cached .env.local.bak .env.preview .env.vercel .env.production.vercel`
- [x] 데이터 덤프 파일 5종 git 추적 해제
  - `git rm --cached db.json dev_notes_rows.json src/data/mes_process_inspections.json scripts/staging_users_backup.json scripts/staging_weekly_reports_backup.json`
- [x] 생성 캐시 `graphify-out/` 하위 전체 파일 git 추적 해제
  - `git rm -r --cached graphify-out/`
- [x] 스크래치 `scratch/` 디렉토리 하위의 파일 전체 git 추적 해제
  - `git rm -r --cached scratch/`
- [x] 옵시디언 에디터 설정 및 캐시 파일들 git 추적 해제 (wiki 콘텐츠 제외)
  - `git rm --cached .obsidian/*.json` (최상위 json 14개 및 한글 clipper 와일드카드 해제)
  - `git rm --cached .obsidian/*핵심*맥락.MD` (한글 와일드카드 해제)
  - `git rm -r --cached .obsidian/.obsidian/`
  - `git rm -r --cached .obsidian/raw/`
  - `git rm -r --cached .obsidian/output/`
  - `git rm -r --cached .obsidian/graphify-out/`

### Phase 2. 소스코드 하드코딩 중앙화
- [x] `src/config/config.js` 신규 생성 (LOCAL_API_URL 선언)
- [x] `src/components/PostApproval.jsx` 수정 (LOCAL_API_URL 도입)
- [x] `src/components/DevNotes.jsx` 수정 (DEV 가드를 유지하며 LOCAL_API_URL 도입)

### Phase 3. 중복 및 임시 파일 정리
- [x] 루트의 `HANDOFF.md` 파일 삭제
- [x] `scratch/clean_obsidian_wiki.js` 파일 삭제

### Phase 4. 빌드 및 최종 정합성 검증
- [x] `npm run build` 실행하여 코드 빌드 무결성 검증
- [x] `git ls-files`를 돌려 무시 설정이 완벽히 적용되었는지, 위키 파일 및 `.env.example`은 여전히 추적 중인지 검증
- [x] 로컬 개발 서버 구동을 통한 기능 작동 검증
