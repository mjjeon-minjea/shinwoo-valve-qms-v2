# 🚀 QMS v2 배포 인프라 정비 — 완료보고서 (R0)

**실행 기준**: R3 구현계획서 (`2026-06-05_QMS_배포인프라_정비_구현계획서_R3.md`)
**실행일**: 2026-06-05
**실행자**: 안티그래비티

---

## ① 반영 내역 요약

### W1 — 정체불명 prod DB 규명 + 메인 좌표 확정
- `qrmyhuipfkctgvzgdvmd` DB를 **폐기/일시정지 인스턴스**로 확정 (TCP False + 폭파사건 보고서 근거).
- `.env.local` 교정: `PROD_DB_URL` 삭제, `PROD_SUPABASE_URL` + `PROD_SERVICE_ROLE_KEY` 신설.
- 테스트 DB 소속 `SUPABASE_SERVICE_ROLE_KEY` 주석 명시 (`ref=srzaanvojyhwzugoaimk`).

### W2 — staging 브랜치 정식화
- `git push -u origin staging` 실행, `remotes/origin/staging` 원격 등록 확인.

### W3 — 테스트웹 Vercel 재배선
- Vercel 대시보드에서 구 레포 Disconnect → `shinwoo-valve-qms-v2` 레포 재연결.
- Production Branch를 `main` → **`staging`** 으로 변경.
- 도메인 `shinwoo-valve-qms-testweb.vercel.app` 정상 접속 확인.

### W5 — 로컬 격리 점검 + 레거시 정리
- `src/` 내 `createClient` 호출: `api.js:6` 단 1곳, `VITE_SUPABASE_URL`만 참조 (하드코딩 0건).
- 레거시 4종 삭제: `test-prod-db-conn.js`, `test-prod-db-conn.cjs`, `check-weekly-reports.js`, `migrate-weekly-reports.js`.

### W6 — 데이터 복제 흐름 구축 + 스키마 동기화
- `sync-db.js` 주석 교정 (40·68·81행: "메인 DB" → "테스트 DB").
- `clone-db.js` 신규 작성 (119행): Proxy 읽기전용 래퍼 + 9종 테이블 타겟팅.
- **테스트 DB 스키마 동기화**: `process_inspections` 테이블에 누락 컬럼 4종 추가 (Supabase SQL Editor).
  ```sql
  ALTER TABLE process_inspections ADD COLUMN IF NOT EXISTS title text;
  ALTER TABLE process_inspections ADD COLUMN IF NOT EXISTS inspector_id uuid;
  ALTER TABLE process_inspections ADD COLUMN IF NOT EXISTS status text DEFAULT 'pending';
  ALTER TABLE process_inspections ADD COLUMN IF NOT EXISTS created_at timestamptz DEFAULT now();
  ```
- **clone-db.js 최종 실행**: 9종 테이블, 총 2,801건 복제 완료.

### W7 — 구유물 정리
- `package.json`: `homepage`, `predeploy`, `deploy` 스크립트 삭제.
- `vite.config.js`: GH Pages 분기 코드 소거, `base: '/'` 단일화.
- `git remote remove test-origin` 실행, `origin`만 잔존.

### Task 8 — .gemini KI stale 진단
- `staging_status.md` (2026-04-16), `ai_rules_backup` (2026-04-21) — 자동 재생성 루프 비활성 입증.

---

## ② 반영 결과 요약

| 워크아이템 | 상태 | 핵심 증빙 |
|---|---|---|
| W1 정체규명 + 좌표확정 | ✅ 완료 | TCP False, `.env.local` diff |
| W2 staging 브랜치 | ✅ 완료 | `git branch -a` 출력 |
| W3 Vercel 재배선 | ✅ 완료 | Branch Tracking = `staging` 스크린샷 |
| W4 메인웹 Vercel 확인 | ⬜ 예정 | 차장님 수동 확인 영역 |
| W5 로컬 격리 + 레거시 삭제 | ✅ 완료 | `git status(deleted)` 4건 |
| W6 데이터 복제 + 스키마 동기화 | ✅ 완료 | clone-db 로그 2,801건, ALTER TABLE Success |
| W7 구유물 정리 | ✅ 완료 | `package.json`·`vite.config.js` diff |
| Task 8 KI stale 진단 | ✅ 완료 | 타임스탬프 실측 데이터 |

### clone-db.js 최종 실행 로그
```
📋 users ... ✅ 6건 복제 완료
📋 weekly_reports ... ✅ 51건 복제 완료
📋 inspections ... ✅ 1,000건 복제 완료
📋 item_master ... ✅ 1,000건 복제 완료
📋 process_inspections ... ✅ 744건 복제 완료
📋 calendar_events ... (0건 — 빈 테이블)
📋 inquiries ... (0건 — 빈 테이블)
📋 resources ... (0건 — 빈 테이블)
📋 suggestions ... (0건 — 빈 테이블)
🎉 총 2,801건 복제 | prodDb 쓰기 시도: 0건 (Proxy 물리 차단 활성)
```

### 인간 통제 영역 사인오프

| 작업 | 사전 보고 | 승인 | 실행 |
|------|----------|------|------|
| `git push -u origin staging` | ✅ | ✅ | ✅ |
| 레거시 `Remove-Item` 4종 | ✅ | ✅ | ✅ |
| `git remote remove test-origin` | ✅ | ✅ | ✅ |

---

## ③ 검증 계획 요약

| # | 검증 항목 | 방법 |
|---|----------|------|
| 1 | 빌드 무결성 | `npm run build` 성공 확인 |
| 2 | 린트 통과 | `npm run lint` Error 0건 확인 |
| 3 | Locked Surface 무변경 | `git diff HEAD` 대상 4파일 변경 0건 |
| 4 | 메인 DB 쓰기 0건 | clone-db Proxy 차단 로그 확인 |
| 5 | 테스트웹 사이트 접속 | `shinwoo-valve-qms-testweb.vercel.app` 정상 렌더링 |
| 6 | 테스트 DB 스키마 동기화 | ALTER TABLE 4문 `Success. No rows returned` |

---

## ④ 검증 결과 요약

| # | 검증 항목 | 결과 | 비고 |
|---|----------|------|------|
| 1 | `npm run build` | ✅ 성공 | 2,843 modules transformed, `dist/index.html` 생성 |
| 2 | `npm run lint` | ✅ 통과 | Error 0건, Warning 47건 (≤ max 50) |
| 3 | Locked Surface 무변경 | ✅ 확인 | `.eslintrc.cjs`, `04_harness_constraints.md`, `verify-integration.js`, `check-structure.js` 무변경 |
| 4 | 메인 DB 쓰기 차단 | ✅ 0건 | Proxy 래퍼 `insert/update/upsert/delete` 물리 throw |
| 5 | 테스트웹 접속 | ✅ 정상 | QMS 로그인 페이지 렌더링 확인 |
| 6 | 스키마 동기화 | ✅ 성공 | `process_inspections` 4컬럼 추가 후 744건 복제 성공 |

---

## 잔여 작업 (차장님 수동)

| # | 작업 | 상태 |
|---|------|------|
| 1 | 메인 DB 비밀번호 로테이션(재발급) — Supabase 대시보드 | 예정 |
| 2 | W4: 메인웹 Vercel 설정 확인 + 캡처 | 예정 |
| 3 | W7: 구 GitHub Pages 비활성화 + 404 캡처 | 예정 |
| 4 | Task 8: `.gemini` KI 타임스탬프 직접 대조 | 예정 |
